# 迟到保护改造计划

> 整理时间：2026-09-04
> 结论来源：`autolib-scheduler` 容器日志（2026-08-03 ~ 2026-09-04）+ MongoDB `AutoLib` 库实测

---

## 一、问题现状：迟到保护的「到馆判断」是空转的

### 1.1 代码上有三道闸门

`backend/scheduled_task.py:818` `late_protect_action()`：

| 闸门 | 位置 | 判断依据 |
|---|---|---|
| 1 | `scheduled_task.py:843` | 手动「我已到馆」标志 `arrived_date == 今天` |
| 2 | `scheduled_task.py:884-898` | 实时查 `resvStatus`，命中 `{1093 使用中, 3141 暂离}` 则跳过 |
| 3 | `scheduled_task.py:633-636` | 扫描/复查检测到签到后回写 `arrived_date`，喂给闸门 1 |

### 1.2 但实测数据显示闸门 2 从未生效

一个月内保护动作共被调用 **51 次**：

| 环节 | 次数 |
|---|---|
| 闸门 1 拦下（手动「我已到馆」） | 16 |
| 进入正式流程（`开始处理座位`） | 35 |
| **闸门 2 拦下（`用户已在馆内`）** | **0** |
| 闸门 2 放行（`用户未签到`） | 28 |
| 成功取消原预约 | 28 |
| 卡在取消失败 / 登录失败 | 7 |

- 9 个用户 `late_protection: "True"`，`late_protection_count` 合计 **28**，与「成功取消」完全吻合。
- **28 次状态码全部是 `1027`（待签到），一个月里没有出现过一次 1093 / 3141。**
- 逐用户核对：每个人的 `用户未签到` 次数与 `成功取消原预约` 次数**完全相等**。

```
28  用户未签到（状态码: 1027）
 0  用户已在馆内
```

### 1.3 原因

保护任务注册在 `scheduled_task.py:1024`：

```python
exec_time = begin_time - timedelta(minutes=7)
```

跑在预约开始前 7 分钟，那时图书馆根本不允许签到，状态恒为 1027。
**闸门 2 是一个结构性空转的分支**，唯一真正起作用的是用户手动点「我已到馆」。

而那 16 次手动跳过只来自 4 个用户（其中一人独占 8 次），另外 5 个开了保护的用户从未点过——**对他们来说迟到保护是 100% 必触发**。

---

## 二、根因：决策窗口被锁死在预约开始前

关键约束（来自实际使用者）：

> 假设预约 8:00，**8:00~8:30 是签到宽限期，此期间预约无法取消**，只能提前取消再重新预约。

日志中大量 `删除座位失败: 预约在当前状态下不能删除` 与此吻合。

因此「把触发点挪到开始之后再判断」的方案**不成立**。真实的问题是：

> 必须在 **8:00 之前**、在图书馆服务器尚无任何签到信息时，**预测**用户今天会不会来。

服务端拿不到任何信号，只能从客户端或历史数据要。

---

## 三、本次要做的

### A. 服务端：到馆时间自学习（优先，可独立验证，用户零感知）

**思路**：迟到保护本质是在补偿「预约时间约早了」。与其每天触发保护，不如让预约时间贴合用户真实作息，从根上消除触发场景。

**数据源**：`visit_logs`（`planned_begin` / `checkin_detected_at`）

**步骤**：

1. **先提升采样精度**（必做前置）
   - `scan_and_record_visits()` 的 docstring（`scheduled_task.py:1212`）写的是「每 15 分钟由调度器调用」，
     但实际注册在 `scheduler_runner.py:177-183` 是 `hour='8,10,12,14,16,18,20,22', minute=0`，**每 2 小时才跑一次**。
   - 导致 `checkin_detected_at` 与真实刷卡时间误差最大 2 小时，不足以做作息统计。
   - 改为 15 分钟一次（注意评估对图书馆接口的请求压力，用户数 ×4/小时）。

2. **攒 2 周数据后做统计**
   - 对每个用户计算「实际到馆时刻 − 预约开始时刻」的分布。
   - 区分两类人：真·偶尔迟到（保护有价值） vs 系统性约早了（应该改预约时间）。

3. **产出**
   - 保守版：在 App / 邮件里给建议——「你最近 10 次平均 9:50 才到，要不要把明天改成 10:00 开始？」
   - 激进版：自动调整明日预约开始时间（需开关，默认关）。

**优势**：纯服务端，本机就能跑数据验证，**不需要任何在校人员配合调试**。

---

### B. 客户端：07:40「一天一问」本地通知（做 APK 时顺手加）

**思路**：不猜用户来不来，直接问。

```
07:40  App 本地闹钟 → 查到今天 8:00 有座位
       → 弹本地通知「今天 8:00 的座位还要吗？」[要] [延后]
07:41  用户点「要」→ POST /api/my/accounts/<pid>/arrived
07:53  服务端闸门 1 读到 arrived_date → 跳过保护
```

**为什么选它**：

- **零新权限**：通知权限 App 已有，不碰定位、不碰 Wi-Fi。
- **复用现成代码**：App 已有「每天 07:05 查抢座结果并弹本地通知」的定时机制，复制一份改时间和文案即可。
- **服务端几乎不用改**：`/api/my/accounts/<pid>/arrived`（`backend/main.py:773`）已存在，
  闸门 1（`scheduled_task.py:843`）已在读 `arrived_date`。语义从「我已到馆」放宽为「我今天会来」即可。
- **零回归风险**：通知没弹 / 用户没点 = 完全等同于现在的行为。对远程发版极其重要。
- **故障可描述**：用户唯一能遇到的问题是「早上没收到通知」，非程序员也说得清。

**已知小缺陷**（属现状，非新增）：`arrived_date` 是全天标志，点一次当天所有时段的保护都会跳过。

**触发时间**：固定 07:40 最简单；更好的是 `预约开始时间 − 20min`，App 本地已知预约时段，实现成本差别不大。

---

## 四、暂缓：Wi-Fi BSSID 到馆检测

**方案概要**（留档，以后有在校测试者再捡起来）：

- **原理**：BSSID 是单台 AP 的 MAC 地址，能精确区分「图书馆那几台 AP」和「宿舍的 AP」；SSID（网络名）全校可能共用，不够用。
- **时间线**：07:50 手机自查当前 BSSID → 命中白名单 → 上报服务端 → 07:53 闸门 1 跳过保护。
- **白名单自学习**：用户首次在图书馆打开 App 时弹窗「检测到你连着 XXX，记为图书馆网络？」，用户确认后入库。用的人多了白名单自己长出来，还能自动适配不同校区楼层。
- **服务端改动**：新增「在馆心跳」字段（**带时间戳，只认最近 10 分钟内的**，防止昨天连过也算数）+ 上报接口 + BSSID 白名单表；闸门 1 从「只看手动 flag」扩成「手动 flag 或新鲜心跳」。

**暂缓理由（关键）**：

1. **维护者已毕业、不在学校**，无法现场调试。
2. **测试者是无编程经验的在校用户**，只能反馈「没反应」。
3. 该方案**失败模式全是静默的**：定位权限被拒、Android 9+ 后台拿到打码值（SSID 变 `<unknown ssid>`）、国产 ROM 杀后台。
   这三种情况在用户侧的表现完全一样，远程无法区分。

**若将来重启，必须先解决的技术点**：

- Android 10+ 获取 SSID/BSSID **必须有定位权限且系统定位开关打开**（用户会疑惑，授权页要写明「只判断是否在图书馆，不上传坐标」）。
- Android 9+ 后台无法获取 Wi-Fi 信息，需用 `AlarmManager` 精确闹钟唤醒 + 几秒钟前台服务读完即退。
- 零权限降级方案：尝试直连图书馆座位系统内网地址，通 = 在校园网内。不需任何权限，但只能判断「在校内」，精度差。

---

## 五、顺带发现的 Bug（独立于上述改造，建议一并修）

### 5.1 `owned_seat` 存在重复条目 → 同一座位同一时刻注册多个 job

`job_id = f"{pid}_{dev_name}_{seat_dict['uuid']}"`（`scheduled_task.py:1029`）带 uuid，
重复条目 uuid 不同，去重失效。

2026-09-04 13:53 实例，同一用户同一座位同时起了 3 个任务：

```
13:53:00 开始处理座位 2F-B061 ...   ×3
13:53:04 取消原预约失败: 预约在当前状态下不能删除   ← 陈旧 uuid
13:53:04 用户未签到（状态码: 1027）→ 成功取消 → 重约 15:00
13:53:14 取消原预约失败: 预约在当前状态下不能删除   ← 陈旧 uuid
```

统计中那 7 次「取消/登录失败」基本都是这类僵尸任务。

**修法**：`owned_seat` 写入时去重；`job_id` 不带 uuid（用 pid+座位+时段）。

### 5.2 闸门 2 在 uuid 未匹配时**静默放行**

`scheduled_task.py:888-898` 的循环只在 uuid 匹配时打日志和判断；
若该 uuid 已不在实时预约列表中（如陈旧条目），循环结束后**不打任何日志、直接落到取消逻辑**。

**修法**：未匹配到 uuid 时应保守跳过并记 warning，而不是继续取消。

### 5.3 自动午休重约的新预约未标记 `by_protection`

2026-09-04 12:00 日志显示，午休重约产生的 14:00 新预约没有 `by_protection` 标记，
随即被迟到保护在 13:53 重新盯上并再次取消重约。

**修法**：`auto_nap_action` 重约成功后比照 `_mark_seat_by_protection()` 打标。

### 5.4 `resvStatus` 类型比较不一致（暂不影响，建议统一）

- `scheduled_task.py:891`：直接 `current_status in {1093, 3141}`，未做 `int()` 转换
- `scheduled_task.py:709`：做了 `int()` 兜底
- `_format_reservation_data`（`utils/library_system.py:1174`）中 `owned_seat` 用 `str()`，`formatted_data` 保留原值

目前上游返回 int（前端 `app.js:1147` 用 `=== 1093` 严格比较且工作正常），暂无实际影响，但两处写法不一致，建议统一加 `int()` 兜底。

---

## 六、工程原则（针对「远程维护 + 非程序员测试」）

1. **任何新功能都必须无回归**：失败时行为退化到现状，而不是变得更糟。
2. **别指望用户描述现象**：App 内加「诊断」页，把关键状态（上次上报时间、服务端返回、当前配置）直接显示出来，让用户**截图**发回。非程序员截图没问题，描述现象是灾难。
3. **客户端异常静默回传服务端**：维护者在服务器上 `grep` 即可定位，不必往返询问。
4. **优先做纯服务端、可本地验证的改动**（如方案 A），把依赖现场调试的改动（如 Wi-Fi）排到最后。

---

## 七、执行顺序建议

| 顺序 | 事项 | 依赖 | 可否本地验证 |
|---|---|---|---|
| 1 | 修 5.1 / 5.2 / 5.3 三个 bug | 无 | ✅ |
| 2 | A-1：`scan_and_record_visits` 提频到 15 分钟 | 无 | ✅ |
| 3 | A-2：攒 2 周数据后做作息统计 | 等待数据 | ✅ |
| 4 | B：客户端 07:40 一天一问 | 做 APK 时 | ⚠️ 需真机 |
| 5 | A-3：建议 / 自动调整明日预约时间 | A-2 结论 | ✅ |
| — | Wi-Fi BSSID 方案 | 需在校测试者 | ❌ 暂缓 |
