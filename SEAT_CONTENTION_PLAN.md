# 座位竞争度采集 + 抢座队列动态调度

> 整理时间：2026-09-09
> 结论来源：`autolib-scheduler` 容器日志（2026-09-07 ~ 09-09）+ MongoDB `AutoLib` 实测
> + 对 `ic-web` 接口的实测探针

**当前状态**：
- 预约尝试记录**已上线**，每次下单都落 `reserve_attempts`（主数据源）
- 全馆占用快照**已上线**（06:45 / 07:01 / 07:05）
- 调度算法已写好但**故意没有接线**，只能手动跑 CLI 出报告。别以为它在生效。
- 选座回归模型**还没写**，见第七节

---

## 一、起因：2026-09-09 两个账号没抢到

20 个账号里 18 个成功，失败的是 `2310104222`（08:00-22:00）和 `2310210301`（09:30-22:00）。

两人的备选座位在 07:00 那一秒全部返回「设备在该时间段内已被预约」：

| 账号 | 第 1 发 | 第 2 发 |
|---|---|---|
| 2310104222 | 07:00:00.223 → 2F-B070 已被占 | 07:00:05.394 → 2F-B033 已被占 |
| 2310210301 | 07:00:00.210 → 2F-B078 已被占 | 07:00:05.466 → 2F-A441 已被占 |

不是系统故障：登录、预登录、时段解析都正常，也不是我们自己人抢自己人
（当天 20 个账号拿到的座位里没有这四张）。就是被校内其他人抢先了。

### 1.1 顺带发现：当天服务端卡了 5 秒

| 日期 | 首个预约请求发出 | 返回 | 耗时 |
|---|---|---|---|
| 09-07 | 07:00:00.180 | 07:00:00.559 | 0.38s |
| 09-08 | 07:00:00.528 | 07:00:01.125 | 0.60s |
| **09-09** | 07:00:00.223 | **07:00:05.392** | **5.17s** |

09-09 第一波 7 个请求**全部卡到 07:00:05.4 前后同时返回**，而 05.9 之后发出的
第二波请求只要 0.2 秒——像是服务端把开闸瞬间涌进来的请求排进队列后统一放行。

推论：我们 07:00:00.2 就进队了仍被抢先，说明排在我们前面的人是在 **07:00:00 之前**
就把请求发出去的。**按约定不动这块**（7:00 开闸是确定的，禁止修改最初的预约时间），
仅记录。

---

## 二、7:00 固定的前提下，还能动的只有两件事

| 旋钮 | 位置 | 影响 |
|---|---|---|
| `priority` | `user_config_info` 字段 | 决定进第几波：`scheduled_task.py:271` 按它降序提交线程池 |
| `seat_list` 顺序 | `user_config_info` 字段 | 决定唯一那次「早期射击」打在哪张座位上 |
| `RESERVE_CONCURRENCY` | 环境变量，默认 8 | 决定第一波容纳多少人 |

**现状是这两件事都没人管**：20 个账号 `priority` 全是 0，排序退化成 `updated_at` 降序，
等于「谁最近在网页上改过配置谁先抢」。

第二备选的请求要等第一发的响应回来才发得出去（09-09 等了 5.2 秒），所以
**第二备选不该是「第二热门的座位」，而该是保底的冷门座位**。09-09 两人的备选恰恰
都是同一片热门区，等于两发子弹打同一个方向。

> **红线**：禁止修改用户信息，`seat_list` 和 `priority` 都算在内。
> 所以现阶段这些结论只能出报告给人看，不能自动生效。

---

## 三、采集端（已上线）

`backend/utils/seat_snapshot.py`

### 3.1 数据源

实测确认 `GET ic-web/reserve?roomIds={roomId}&resvDates=D,D&sysKind=8` 一次返回该区域
**所有**座位当天的预约区间：

```
resvInfo: [{ uuid, resvId, startTime(ms), endTime(ms), resvStatus, devId,
             trueName: null, logonName: null }]
```

不含预约人身份。另有 `devStatus` / `openState` 标记座位是否停用，
`openStart` / `openEnd` 是 `"07:30"` 这样的**字符串**（不是时间戳，踩过一次）。

顺带确认了 `resvRule`：`earliestResvTime=1860`（31 小时）、`minResvTime=120`、
`maxResvTime=900`、`timeInterval=30`。

### 3.2 规模与代价

12 个区域 / 2749 个座位，一轮约 14 秒。观测账号 `2000102115`（`SEAT_SNAPSHOT_PID`）。
**只发 GET，不写任何用户配置。**

2026-09-10 首张快照（09-09 手工拍摄，tag `manual1030`）：

| 区域 | 座位 | 已被占 |
|---|---|---|
| 二层A区 | 441 | 308 |
| 二层B区 | 96 | 71 |
| 三层A区 | 404 | 333 |
| 三层C区 | 162 | 131 |
| 三楼夹层 | 20 | 20 |
| 四层A区 | 428 | 286 |
| 四楼夹层 | 24 | 24 |
| **五层A区** | 360 | **29** |
| **六层** | 344 | **31** |
| **七层北侧** | 224 | **8** |
| 七层南侧 | 114 | 114 |

竞争完全集中在二~四层。五层、六层、七层北侧基本没人抢。

### 3.3 存储

集合 `seat_snapshots`，一条文档 = 一天一个区域一次拍摄：

```json
{ "date": "2026-09-10",        // 被观测的那一天，注意是未来日期
  "weekday": 4, "tag": "0705", "captured_at": ISODate,
  "room_id": "100455346", "room_name": "二层B区", "floor_name": "图书馆二层",
  "open": [450, 1320],
  "seats": { "2F-B070": [[450, 1320]], "2F-B068": [] },   // 分钟偏移
  "unavailable": ["2F-B050"],
  "stats": { "seats": 96, "booked_seats": 71, "intervals": 72,
             "status_hist": {"1027": 72} } }
```

唯一索引 `(date, tag, room_id)`，重跑同一天同一 tag 是覆盖不是攒重复。

### 3.4 关键实测：开闸前板子是空的

2026-09-09 10:50 查 **09-11**（要到 09-10 才开放）的占用：

```
12 个区域 / 2749 座位 / 0 已被占
```

一条预约都没有。所以一天的座位在它开放之前一张都订不出去，
**1400 多条预约全挤在 07:00~07:05 这五分钟里**。

这条实测直接决定了采集的设计：拍在 7 点前后几小时都是同一个数，没有任何信息量，
分辨率必须做到分钟级。

### 3.5 调度：一个早上三张

| 时刻 | tag | 意义 |
|---|---|---|
| 06:45 | `pre` | 抢座前，板子应该还是空的；也是「有没有人比 7:00 更早下手」的对照 |
| 07:01 | `rush` | 7:00 那一波刚结束——**这张才是「谁抢赢了」的真信号** |
| 07:05 | `post` | 再加上几分钟慢慢约的人 |

```
rush − pre  = 7:00 那一波真抢走的座位  → 我们抢不过的
post − rush = 07:01 之后慢慢约走的     → 我们本来拿得到的
```

只拍 post 会把这两类混成一个数，竞争度算高——本来抢得到的座位会被误判成抢不过。

时刻都由 `SCHEDULE_HOUR/MINUTE` 推算（`SEAT_SNAPSHOT_PRE_MINUTES` /
`SEAT_SNAPSHOT_RUSH_SECONDS` / `SEAT_SNAPSHOT_DELAY_MINUTES`），没有动 7:00 本身。
tag 用固定字面量而非执行时刻，晚几分钟跑也归到同一批数据。
`SEAT_SNAPSHOT_ENABLED=0` 可关。

**抢座窗口里不产生任何新登录**：`rush` 那张复用 06:45 建好的会话
（`seat_snapshot.login()` → `capture(library=...)`）。拿不到会话就干脆不拍——
宁可缺一张，也不跟抢座抢网关。这段交接逻辑挂在模块级全局上，错了不会报错、
只会静默地再也拍不到 rush，所以 `tests/test_seat_allocator.py` 里有三条测试钉着它。

---

## 三·五、预约尝试记录（已上线，主数据源）

`backend/utils/attempt_log.py` → 集合 `reserve_attempts`

### 3.5.1 为什么它比快照更好

**我们自己发出的每一个预约请求，本身就是对那张座位的一次精确探测：**

```
07:00:00.223  开枪 → 2F-B070
07:00:05.392  回报 → 「设备在该时间段内已被预约」
```

含义是「开闸后第 223 毫秒，B070 已经被人拿走了」。精确到毫秒、零新增网络请求，
而且覆盖的正好是用户真正想要的热门座位——最需要数据的那部分。

这份数据我们每天都在产生然后扔掉：以前只进容器日志，会被滚掉。现在留下来。

### 3.5.2 落库位置和代价

写在 `_reserve_single_seat` 的 `finally` 里（成功/失败/抛异常三条路都不漏），
**在拿到图书馆响应之后**才写。所以它不拖慢这一次下单，唯一延后的是循环里
下一张座位的尝试，量级 1 毫秒——而第二发本来就要等第一发响应 5 秒多。
任何异常都被吞掉，有两条测试钉着「数据库挂了也不能把抢座搞挂」。

字段：`target_date / weekday / pid / seat_name / dev_id / begin / end /
begin_min / end_min / fired_at / responded_at / latency_ms / offset_ms /
phase / outcome / message`。

`offset_ms` = 开枪时刻距**当天 7:00** 多少毫秒，就是模型要的时间轴。

### 3.5.3 outcome 分类：只有一类能当竞争信号

| outcome | 含义 | 能否用于建模 |
|---|---|---|
| `success` | 我们拿到了 → 那一刻座位活着 | ✅ |
| `taken` | 「设备在该时间段内已被预约」 | ✅ **唯一的竞争证据** |
| `self_conflict` | 「学工号为：xxx的用户在当前时段有预约」 | ❌ 撞的是自己的单子 |
| `out_of_window` | 超出提前预约窗口 | ❌ 我们自己发早了 |
| `network` | 网络/状态码异常 | ❌ 没问到座位 |

`self_conflict` 是 2026-09-09 端到端验证时实测撞出来的：账号自己在该时段已有预约时，
图书馆**先报这个，根本不去看座位空不空**。这一类混进竞争统计会把结果搞烂，
所以单列一类，`CONTENTION_OUTCOMES` 只含 `success` 和 `taken`。

---

## 四、调度算法（已写好，**未接线**）

`backend/utils/seat_allocator.py`

### 4.1 第一步：竞争度估计

模型默认吃 `rush` 那张（没有时退回 `post`）：要预测的是「7:00 那一波我抢不抢得到」，
`rush` 正好定格在那一波刚结束。

对每张座位、每个目标时段，统计历史上「抢座那一波结束后还空着」的加权频率：

```
w(day) = 0.5 ^ (距目标日天数 / HALF_LIFE_DAYS) × (同星期几 ? WEEKDAY_BONUS : 1)
p(seat) = (Σ w·free + α·区域先验) / (Σ w + α)          α = PRIOR_STRENGTH
```

样本少时靠**同区域整体空闲率**做 Beta 平滑，所以没观测过的座位不会因为
「没数据」被判成必胜或必败。攒满一个月加权样本量约 14.7，`α=2` 时先验约占 12%。

用 09-10 那张快照实测（把它当历史、目标日设 09-11）：

```
二层B区 08:00-22:00 区域先验 0.266
  2F-B070 / B033 / B078 / B072   0.180      ← 09-09 抢失败的那几张
  2F-B068 / B065 / B059          0.502      ← 隔壁，整天没人订
```

B068、B065 的 `devProp/devStatus/openState/onlyView` 与 B070 完全一致，
**是能约的**，只是没人要。

### 4.2 第二步：座位分配（二分图最大权匹配）

左边账号、右边候选座位，边权 = 偏好度 × 存活概率，一张座位只分给一个账号。
纯 Python 实现的 O(n²m) 匈牙利（JV 版），规模 20 × 几百，毫秒级。
**不引入 scipy**——这个项目现在一个数值库都不依赖。

这条约束顺带根治「自己人打自己人」：09-09 之后 `2310104222`、`2310306123`、
`2310102129` 三个账号都把 `2F-B036` 放进了前两位，时段互相重叠。

### 4.3 第三步：队列排序

```
priority = 1000 × (1 - max(自己备选的存活概率)) × 严重度
严重度 = 1 + 0.5·迟到保护 + 0.3·(时段长度/14h)
```

**风险必须按账号自己配置的座位算，不能按分配建议的座位算**——座位配置改不了，
7:00 真正发出去的还是 `seat_list` 里那几张。（第一版写错过，热门座位账号被排到了最后一波。）

用 `1 - max(p)` 而不是 `∏(1-p)`：同区域座位是一起被抢光的，成败强相关，连乘会把风险算得离谱地低。

### 4.4 红线守卫

`apply_plan()` 直接抛 `NotImplementedError`，`tests/test_seat_allocator.py`
里有一条测试盯着它。报告写进独立的 `seat_plans` 集合，**绝不碰 `user_config_info`**。

---

## 五、接线前必须先解决的问题

1. **pre/rush/post 的差还没喂进模型的目标变量**。`rush_delta()` 现在只用于人看的
   诊断报告；`survival()` 用的仍是「rush 那张里空不空」这个二值目标。
   更准的做法是直接拿 `rush − pre` 当训练目标。接线前应该改掉。
2. **冷门座位为什么冷门，快照答不了**。可能没人要，也可能靠门/没插座/屏幕坏。
   真推荐给人之前得实地确认几张。
3. **自己人内耗没进模型**。k 个账号盯同一张座位时，至少 k-1 个会输，
   但风险估计里没体现这一层。
4. **分配结果目前无法生效**。红线在，只能出报告。要放开需要用户明确改约定。
5. **数据量**。至少攒 7 天再看报告，少于 7 天时 `format_report` 会打警告。

---

## 六、怎么用

```bash
# 手动补拍一张快照（默认拍明天）
docker compose exec scheduler python -m utils.seat_snapshot
docker compose exec scheduler python -m utils.seat_snapshot 2026-09-12 --tag pre

# 看 7:00 那一波到底抢走了什么（需要当天有 pre/rush/post 三张）
docker compose exec scheduler python -m utils.seat_snapshot --rush
docker compose exec scheduler python -m utils.seat_snapshot --rush --window 08:00-22:00

# 看分配报告（不改任何配置）
docker compose exec scheduler python -m utils.seat_allocator
docker compose exec scheduler python -m utils.seat_allocator --days 30 --save

# 跑测试
docker run --rm -v $PWD/backend:/app:ro -w /app autolib-flask-api python -m tests.test_seat_allocator
```

---

## 七、下一步：选座回归模型（还没写）

目标：预测 **P(座位 s 在开闸后 15 秒内还没被别人抢走)**，据此给用户出「建议座位」
作为参考。按红线，只是建议，不自动改任何配置。

### 7.1 两份数据形状不同，互补

| | 精度 | 覆盖 | 每天样本 |
|---|---|---|---|
| `reserve_attempts` | **毫秒级时间轴** | 只有用户配的那 ~40 张 | 20~50 |
| `seat_snapshots`（07:01） | 60 秒粒度的二值 | **全部 2749 张** | 2749 |

前者有真正的时间轴但覆盖窄，后者覆盖全但粒度粗。两者都要。

### 7.2 为什么要回归，而不是只统计每张座位的历史比例

按座位单独统计，一张座位一天只有一个样本，攒够要一个月；而且对**从没试过的座位**
完全没有预测力——恰恰是「建议座位」最需要回答的那些。

回归的价值在于**跨座位泛化**：学出「三层A区、周一、08:00 开始的长时段特别抢手」
这种规律，然后套到没有样本的座位上。

### 7.3 特征

- 区域（12 个 one-hot）、楼层 —— 目前看效应最大：二~四层几乎满，五~七层没人要
- 请求起始时间 `begin_min`、时长、`weekday`
- **座位坐标** —— `backend/static/floorplans/seat_layout.json` 里有全部 2749 张座位的
  x/y 百分比坐标。可派生：离区域中心的距离、是否靠边（靠窗/靠墙/角落）、
  与最近邻座位的距离。**这是真正能泛化的特征**，能解释「为什么 B070 热而隔壁 B068 冷」
- 该座位自身的历史比例（当特征，不当唯一依据）
- 邻座的历史比例（空间平滑）

### 7.4 实现

纯 Python 逻辑回归（梯度下降，约 60 行），**不引入 scipy / sklearn**——
这个项目现在一个数值库都不依赖，为一个模型加 100MB 依赖不划算。
规模 2749 座位 × 30 天 ≈ 8 万行 × 几十个特征，几秒内收敛。

### 7.5 什么时候能用

| 能学出什么 | 需要攒 |
|---|---|
| 区域级差异（哪层没人抢） | 3~5 天 |
| 坐标派生的规律（靠窗/角落效应） | ~2 周（跨座位共享，收敛快） |
| 单张座位的个体差异 | 3~4 周 |

### 7.6 上线前必须做的事

**校准检查**：预测 0.8 的那批座位，实际是不是真有 80% 存活下来。
「建议座位」要能被信任，靠的是概率校准，不是准确率。没做校准检查之前
不要把数字给用户看。
